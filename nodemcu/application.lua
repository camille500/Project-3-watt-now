local request = require 'request'
local wifimodule = require 'wifimodule'



-- Data to send with the request
--local t = { 12394 }
--
---- confert data to json code
--local j = cjson.encode(table)

-- post request
--request.post('https://p3-wottnow.herokuapp.com', '/user/' .. node.chipid(), j, function(res)
--
-- print(res)
--end)
